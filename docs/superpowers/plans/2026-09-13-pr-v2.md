# PR v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rendre les PR crédibles (seuil ≥ 2 séances, kind `volume`, migration historique) et n’afficher qu’une bannière Record synthétique par exercice en séance.

**Architecture:** Règles pures dans `domain/records.ts` ; le repo compte les séances antérieures, calcule les kinds à l’écriture et rejoue après édition ; migration idempotente au démarrage ; UI séance agrège les kinds dans une bannière.

**Tech Stack:** React 19, Dexie, Vitest. Pas de nouvelle dépendance.

## Global Constraints

- Spec : `docs/superpowers/specs/2026-09-13-pr-v2-design.md`
- Seuil : `priorCompletedSessionCount < 2` → aucun PR
- Kinds : `weight` | `reps` | `e1rm` | `volume` (série = weight×reps×unilateralMult)
- Warmup / approach → jamais PR
- Retirer `· Record` de `SetInput` ; bannière unique par exercice
- Setting : `prRulesMigratedV2`
- Branche : `feat/pr-v2` depuis `master`
- Commandes depuis `app/` : `npx vitest run …`, `npm run typecheck`

## File structure

| File | Responsibility |
|---|---|
| `app/src/domain/records.ts` | detectPrKinds v2 + volume + seuil |
| `app/tests/domain/records.test.ts` | Tests (remplacer le cas « 1ʳᵉ série = toujours PR ») |
| `app/src/domain/pr-labels.ts` | Libellés FR courts pour bannière |
| `app/src/repositories/workouts.repo.ts` | count + logSet + replay edit |
| `app/tests/repositories/workouts-pr.test.ts` | Intégration |
| `app/src/db/migrate-pr-v2.ts` | Recalcul historique |
| `app/src/db/schema.ts` | SETTINGS_KEYS |
| `app/src/main.tsx` | Appeler migration |
| `app/src/features/session/ActiveSessionScreen.tsx` | Bannière |
| `app/src/features/session/SetInput.tsx` | Retirer Record ligne |
| `docs/01-…` / `docs/04-…` | RG-16 + formules |

---

### Task 1: Domain records v2

**Files:**
- Modify: `app/src/domain/records.ts`
- Modify: `app/tests/domain/records.test.ts`
- Create: `app/src/domain/pr-labels.ts` (peut être Task 4 ; ici si utile pour tests labels)

**Interfaces:**
- Produces:
  - `export const MIN_PRIOR_COMPLETED_SESSIONS_FOR_PR = 2`
  - `export function setVolumeKg(weightKg: number, reps: number, unilateral: boolean): number`
  - `export function detectPrKinds(candidate, priorSets, ctx: { priorCompletedSessionCount: number; unilateral?: boolean }): PrKind[]`

- [ ] **Step 1: Rewrite failing tests**

Remplacer/étendre `records.test.ts` :

```ts
import { describe, expect, it } from 'vitest';
import { detectPrKinds, MIN_PRIOR_COMPLETED_SESSIONS_FOR_PR } from '../../src/domain/records';

const ctxOk = { priorCompletedSessionCount: MIN_PRIOR_COMPLETED_SESSIONS_FOR_PR };
const history = [
  { weightKg: 80, reps: 10, e1rm: 106.6, isWarmup: false },
  { weightKg: 82.5, reps: 8, e1rm: 104.5, isWarmup: false },
];

describe('detectPrKinds v2', () => {
  it('refuse sous le seuil de séances', () => {
    expect(
      detectPrKinds({ weightKg: 100, reps: 10, e1rm: 130, isWarmup: false }, history, {
        priorCompletedSessionCount: 1,
      }),
    ).toEqual([]);
  });

  it('detecte weight au-dessus du seuil', () => {
    expect(
      detectPrKinds({ weightKg: 85, reps: 8, e1rm: 107.6, isWarmup: false }, history, ctxOk),
    ).toContain('weight');
  });

  it('detecte volume', () => {
    const kinds = detectPrKinds(
      { weightKg: 80, reps: 12, e1rm: null, isWarmup: false },
      history,
      { ...ctxOk, unilateral: false },
    );
    expect(kinds).toContain('volume'); // 80*12=960 > 80*10=800
  });

  it('ignore warmup', () => {
    expect(
      detectPrKinds({ weightKg: 90, reps: 10, e1rm: 120, isWarmup: true }, history, ctxOk),
    ).toEqual([]);
  });

  it('historique vide + seuil non atteint → pas de PR', () => {
    expect(
      detectPrKinds({ weightKg: 20, reps: 10, e1rm: 26.6, isWarmup: false }, [], {
        priorCompletedSessionCount: 0,
      }),
    ).toEqual([]);
  });
});
```

- [ ] **Step 2: Run — expect FAIL** (signature / comportement)

- [ ] **Step 3: Implement**

```ts
export const MIN_PRIOR_COMPLETED_SESSIONS_FOR_PR = 2;

export function setVolumeKg(weightKg: number, reps: number, unilateral: boolean): number {
  return weightKg * reps * (unilateral ? 2 : 1);
}

export function detectPrKinds(
  candidate: RecordCandidate,
  priorSets: PriorSet[],
  ctx: { priorCompletedSessionCount: number; unilateral?: boolean },
): PrKind[] {
  if (candidate.isWarmup) return [];
  if (candidate.reps == null || candidate.reps < 1) return [];
  if (ctx.priorCompletedSessionCount < MIN_PRIOR_COMPLETED_SESSIONS_FOR_PR) return [];

  const working = priorSets.filter((s) => !s.isWarmup && s.reps != null && s.reps >= 1);
  const unilateral = ctx.unilateral ?? false;
  const kinds: PrKind[] = [];

  // weight, reps, e1rm — logique existante
  // volume:
  if (candidate.weightKg != null) {
    const candVol = setVolumeKg(candidate.weightKg, candidate.reps, unilateral);
    const maxVol = Math.max(
      0,
      ...working
        .filter((s) => s.weightKg != null && s.reps != null)
        .map((s) => setVolumeKg(s.weightKg!, s.reps!, unilateral)),
    );
    if (candVol > maxVol) kinds.push('volume');
  }
  return kinds;
}
```

Mettre à jour `isPersonalRecord` pour prendre le même `ctx`.

- [ ] **Step 4: PASS + commit**

```bash
git commit -m "feat(domain): PR v2 threshold and set volume kind"
```

---

### Task 2: Repo — compteur, logSet, replay edit

**Files:**
- Modify: `app/src/repositories/workouts.repo.ts`
- Test: `app/tests/repositories/workouts-pr.test.ts`

**Interfaces:**
- `export async function countPriorCompletedSessions(exerciseId: string, excludeWorkoutId: string): Promise<number>`
- `export async function recomputePrForExerciseChronologically(exerciseId: string): Promise<void>`

- [ ] **Step 1: Tests**

Scénarios (réutiliser seed minimal type tonnage tests) :

1. Première séance completed + 2ᵉ en cours : logSet work lourd → `isPR false`
2. Deux séances completed antérieures + 3ᵉ : charge > max → `isPR true`, `prKinds` contient `weight`
3. `editSetLog` baisse le max → recompute enlève PR sur cette série si plus un max

- [ ] **Step 2: Implement countPriorCompletedSessions**

```ts
// workoutExercises for exerciseId → unique workoutIds completed, deletedAt null, !== exclude
```

- [ ] **Step 3: Wire logSet**

```ts
const we = await db.workoutExercises.get(input.workoutExerciseId);
const priorCount = we
  ? await countPriorCompletedSessions(input.exerciseId, we.workoutId)
  : 0;
const ex = await db.exercises.get(input.exerciseId);
const prKinds = detectPrKinds(candidate, priorSets, {
  priorCompletedSessionCount: priorCount,
  unilateral: ex?.unilateral ?? false,
});
```

Note : `getPriorSetsForExercise` doit **exclure** les sets de la série en cours / ou inclure seulement completed workouts + sets antérieurs dans la séance courante déjà logués. Comportement actuel : toutes les séries de l’exercice y compris séance courante déjà validées — **conserver** (une 2ᵉ série de la séance compare à la 1ʳᵉ).

- [ ] **Step 4: editSetLog + replay**

Après update de la série :

```ts
await recomputePrForExerciseChronologically(exerciseId);
```

Implémentation replay : charger toutes les séries work de l’exercice ordonnées par `completedAt`, pour chaque série recalculer kinds vs priors strictement antérieurs + `countPriorCompletedSessions` basé sur le workout de cette série.

- [ ] **Step 5: PASS + commit**

```bash
git commit -m "feat(workouts): enforce PR v2 on log and edit replay"
```

---

### Task 3: Migration PR v2

**Files:**
- Create: `app/src/db/migrate-pr-v2.ts`
- Modify: `app/src/db/schema.ts` — `prRulesMigratedV2`
- Modify: `app/src/main.tsx`
- Test: `app/tests/db/migrate-pr-v2.test.ts`

- [ ] **Step 1: Test** — série « fausse PR » en 1ʳᵉ séance → après migrate `isPR false` ; 2ᵉ run no-op (setting true, même si on corrompt un flag)

- [ ] **Step 2: Implement** — pour chaque exerciseId distinct dans setLogs, appeler `recomputePrForExerciseChronologically` ; put setting

- [ ] **Step 3: Wire main après `migrateDataReliabilityV1`**

- [ ] **Step 4: PASS + commit**

```bash
git commit -m "feat(db): migrate historical PR flags to v2 rules"
```

---

### Task 4: UI bannière + SetInput

**Files:**
- Create: `app/src/domain/pr-labels.ts` — `formatPrBanner(kinds: PrKind[]): string` → `Record · charge · e1RM`
- Modify: `ActiveSessionScreen.tsx` + css
- Modify: `SetInput.tsx` — retirer `{log.isPR ? … Record}`

**Labels FR :**

```ts
{ weight: 'charge', reps: 'reps', e1rm: 'e1RM', volume: 'volume' }
```

- [ ] **Step 1: State**

```ts
const [prBanner, setPrBanner] = useState<{ kinds: PrKind[] } | null>(null);
```

Après `logSet`, si `set.prKinds.length` :  
`setPrBanner({ kinds: union(prBanner?.kinds ?? [], set.prKinds) })`  
Sur `goToNextStep` / changement d’exercice : `setPrBanner(null)`.

Afficher bandeau texte discret au-dessus de EffortChips ou sous SetInput.

- [ ] **Step 2: SetInput** — supprimer le span Record

- [ ] **Step 3: typecheck + commit**

```bash
git commit -m "feat(session): synthetic PR banner per exercise"
```

---

### Task 5: Docs + commit spec/plan

**Files:**
- `docs/01-architecture-fonctionnelle.md` — RG-16
- `docs/04-modele-de-donnees.md` — PR volume + seuil
- Spec + plan lot 3

Nouveau RG-16 :

> Un PR n’est validé que sur une série work (`reps ≥ 1`), si ≥ 2 séances completed antérieures existent pour l’exercice. Kinds : charge, reps à charge, e1RM, volume de série.

- [ ] **Step 1: Éditer docs**
- [ ] **Step 2: Commit**

```bash
git commit -m "docs: PR v2 rules (threshold, volume, banner)"
```

---

## Spec coverage

| Requirement | Task |
|---|---|
| Seuil 2 séances | 1–2 |
| Kind volume | 1 |
| logSet / edit replay | 2 |
| Migration | 3 |
| Bannière + retrait ligne | 4 |
| Docs | 5 |

## Placeholder scan

Aucun TBD. Note : le test historique « première série toujours record » **doit** être remplacé (comportement volontairement cassé).
