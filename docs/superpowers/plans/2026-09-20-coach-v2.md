# Coach v2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer la progression « reps atteints → +kg » par un coach RIR-centric avec mémoire par exercice, suggestions HOLD/INCREASE/DECREASE/REPEAT/WATCH, brief pré-séance, hint mid-set, et raisons en français.

**Architecture:** Domaine pur (`load-semantics` + `evaluateExerciseProgression` + `evaluateCoach` étendu) ; table IndexedDB `exerciseProgressionMemory` recalculée (cache dérivé, hors sync) ; surfaces Today / séance / Progression consomment memory + suggestions.

**Tech Stack:** React 19, Dexie, Vitest. Pas de nouvelle dépendance.

## Global Constraints

- Spec : `docs/superpowers/specs/2026-09-20-coach-v2-design.md`
- ADR-003 : moteur déterministe, pas de LLM dans la décision
- Jamais `increase` si la dernière séance work contient une série à RIR 0
- Séries `warmup` / `approach` exclues des décisions
- Memory = cache dérivé : **ne pas** ajouter à `DEXIE_SYNC_TABLES` (recalcul local)
- Commandes depuis `app/` : `npx vitest run …`, `npm run typecheck`
- Branche suggérée : `feat/coach-v2` depuis `master` (créer au Task 1)
- Commits fréquents ; message en français style repo (`feat(coach): …`)

## File structure

| File | Responsibility |
|---|---|
| `app/src/domain/load-semantics.ts` | `LoadSemantics`, comparabilité, delta d’incrément (assistance inversée) |
| `app/tests/domain/load-semantics.test.ts` | Tests comparabilité + delta |
| `app/src/domain/coach.ts` | Types v2 + `evaluateExerciseProgression` + `evaluateCoach` réécrit |
| `app/src/domain/coach-midset.ts` | Hint mid-set pur (série courante → texte) |
| `app/tests/domain/coach.test.ts` | Cas semaine + legacy C-04/C-06/C-07 |
| `app/tests/domain/coach-midset.test.ts` | Hints RIR 0 / RIR élevé |
| `app/src/db/schema.ts` | `Exercise` enrichi + `ExerciseProgressionMemory` + SETTINGS si besoin |
| `app/src/db/db.ts` | Dexie v4 + table `exerciseProgressionMemory` |
| `app/src/data/exercices.seed.json` | `loadSemantics` (+ plages optionnelles) |
| `app/src/db/seed.ts` | Mapper nouveaux champs ; bump `CURRENT_SEED_VERSION` |
| `app/src/repositories/exercise-memory.repo.ts` | `recomputeAllExerciseMemories` / `getMemoriesForExerciseIds` |
| `app/src/repositories/progress.repo.ts` | Brancher coach v2 + persister memory |
| `app/src/repositories/workouts.repo.ts` | Appeler recompute après `completeWorkout` |
| `app/src/features/progress/CoachSuggestions.tsx` (+ css) | Afficher action HOLD + raison + accept hold |
| `app/src/features/today/SessionBrief.tsx` (+ css) | Objectifs du jour |
| `app/src/features/today/TodayScreen.tsx` | Monter le brief |
| `app/src/features/session/ActiveSessionScreen.tsx` (+ css) | Hint mid-set après VALIDER |
| `docs/01-architecture-fonctionnelle.md` | Mettre à jour D7 |
| `docs/adr/003-coach-moteur-de-regles.md` | Noter actions v2 / RIR |

---

### Task 1: `load-semantics` (domaine)

**Files:**
- Create: `app/src/domain/load-semantics.ts`
- Create: `app/tests/domain/load-semantics.test.ts`

**Interfaces:**
- Produces:
  - `export type LoadSemantics = 'external_weight' | 'bodyweight' | 'assistance' | 'machine_resistance'`
  - `export function isLoadHistoryComparable(weightsKg: number[], thresholdRatio?: number): boolean`
  - `export function nextLoadKg(currentKg: number, incrementKg: number, semantics: LoadSemantics, direction: 'up' | 'down'): number`

- [ ] **Step 1: Create branch**

```bash
git checkout master
git checkout -b feat/coach-v2
```

- [ ] **Step 2: Write failing tests**

```ts
import { describe, expect, it } from 'vitest';
import { isLoadHistoryComparable, nextLoadKg } from '../../src/domain/load-semantics';

describe('load-semantics', () => {
  it('détecte un écart > 30 % vs médiane', () => {
    expect(isLoadHistoryComparable([50, 50, 25])).toBe(false);
    expect(isLoadHistoryComparable([65, 65, 60])).toBe(true);
  });

  it('inverse le sens pour assistance', () => {
    expect(nextLoadKg(20, 2.5, 'assistance', 'up')).toBe(17.5); // moins d’assistance
    expect(nextLoadKg(20, 2.5, 'external_weight', 'up')).toBe(22.5);
  });
});
```

- [ ] **Step 3: Run tests — expect FAIL**

Run: `npx vitest run tests/domain/load-semantics.test.ts`  
Expected: FAIL (module missing)

- [ ] **Step 4: Implement**

```ts
export type LoadSemantics = 'external_weight' | 'bodyweight' | 'assistance' | 'machine_resistance';

export function isLoadHistoryComparable(weightsKg: number[], thresholdRatio = 0.3): boolean {
  const weights = weightsKg.filter((w) => w != null && Number.isFinite(w));
  if (weights.length < 2) return true;
  const sorted = [...weights].sort((a, b) => a - b);
  const mid = Math.floor(sorted.length / 2);
  const median =
    sorted.length % 2 === 0 ? (sorted[mid - 1]! + sorted[mid]!) / 2 : sorted[mid]!;
  if (median <= 0) return true;
  const latest = weights[weights.length - 1]!;
  return Math.abs(latest - median) / median <= thresholdRatio;
}

export function nextLoadKg(
  currentKg: number,
  incrementKg: number,
  semantics: LoadSemantics,
  direction: 'up' | 'down',
): number {
  const sign = direction === 'up' ? 1 : -1;
  const delta = semantics === 'assistance' ? -sign * incrementKg : sign * incrementKg;
  return Math.max(0, Number((currentKg + delta).toFixed(2)));
}
```

- [ ] **Step 5: Run tests — expect PASS**

Run: `npx vitest run tests/domain/load-semantics.test.ts`

- [ ] **Step 6: Commit**

```bash
git add app/src/domain/load-semantics.ts app/tests/domain/load-semantics.test.ts
git commit -m "feat(coach): add load semantics helpers"
```

---

### Task 2: Schéma + Dexie v4 (memory)

**Files:**
- Modify: `app/src/db/schema.ts`
- Modify: `app/src/db/db.ts`

**Interfaces:**
- Produces: types `LoadSemantics` réexportés ou importés depuis domain ; `ExerciseProgressionMemory` ; champs optionnels sur `Exercise`

- [ ] **Step 1: Extend `Exercise` and add memory type in `schema.ts`**

Ajouter après les imports utiles :

```ts
import type { LoadSemantics } from '../domain/load-semantics';
export type { LoadSemantics };
```

Sur `Exercise`, ajouter :

```ts
  loadSemantics?: LoadSemantics;
  minReps?: number | null;
  maxReps?: number | null;
  targetRirMin?: number | null;
  targetRirMax?: number | null;
```

Ajouter l’interface :

```ts
export interface ExerciseProgressionMemory {
  exerciseId: string;
  status: 'increase' | 'hold' | 'decrease' | 'repeat' | 'watch';
  currentLoadKg: number | null;
  suggestedLoadKg: number | null;
  targetReps: [number, number];
  targetRir: [number, number];
  confidence: 'high' | 'medium' | 'low';
  consecutiveSuccesses: number;
  consecutiveFailures: number;
  lastReason: string;
  lastEvaluatedAt: string;
  comparableHistory: boolean;
  updatedAt: string;
}
```

- [ ] **Step 2: Dexie v4 in `db.ts`**

Importer `ExerciseProgressionMemory`, ajouter :

```ts
exerciseProgressionMemory!: Table<ExerciseProgressionMemory, string>;
```

Après `version(3)…`, ajouter :

```ts
this.version(4).stores({
  exercises: 'id, name, *primaryMuscles, equipment, updatedAt',
  cycles: 'id, startDate, updatedAt',
  sessionTemplates: 'id, cycleId, code, dayOfWeek, updatedAt',
  prescribedItems: 'id, sessionTemplateId, [sessionTemplateId+order], exerciseId',
  workouts: 'id, date, status, sessionTemplateId, updatedAt',
  workoutExercises: 'id, workoutId, exerciseId, [workoutId+order]',
  setLogs: 'id, workoutExerciseId, [workoutExerciseId+index], completedAt, isPR',
  cardioLogs: 'id, workoutId, date',
  bodyMetrics: 'id, date',
  progressPhotos: 'id, date, pose',
  proteinEntries: 'id, date',
  settings: 'key',
  exerciseProgressionMemory: 'exerciseId, updatedAt',
});
```

Ne pas ajouter la table à `DEXIE_SYNC_TABLES`.

- [ ] **Step 3: Typecheck**

Run: `npm run typecheck`  
Expected: PASS (champs optionnels)

- [ ] **Step 4: Commit**

```bash
git add app/src/db/schema.ts app/src/db/db.ts
git commit -m "feat(db): add exercise progression memory table"
```

---

### Task 3: Moteur `evaluateExerciseProgression` + rewrite `evaluateCoach`

**Files:**
- Modify: `app/src/domain/coach.ts`
- Modify: `app/tests/domain/coach.test.ts`

**Interfaces:**
- Consumes: `LoadSemantics`, `isLoadHistoryComparable`, `nextLoadKg`
- Produces:
  - `CoachAction = 'increase' | 'hold' | 'decrease' | 'repeat' | 'watch' | 'deload' | 'vary' | 'inform'`
  - `CoachSuggestion` enrichi : `confidence`, `status` (alias action pour UI), garder `ruleId` (`C-01`… ou nouveaux `C-10` hold etc. — **utiliser** `C-01` increase, `C-02` decrease, `C-08` hold, `C-09` repeat, `C-10` watch pour traçabilité)
  - `export function evaluateExerciseProgression(input: ExerciseProgressionInput): ExerciseProgressionResult`
  - `evaluateCoach` appelle la progression par exercice + règles globales

**Types à ajouter dans `coach.ts` :**

```ts
export type CoachAction =
  | 'increase'
  | 'hold'
  | 'decrease'
  | 'repeat'
  | 'watch'
  | 'deload'
  | 'vary'
  | 'inform';

export type CoachRuleId =
  | 'C-01'
  | 'C-02'
  | 'C-03'
  | 'C-04'
  | 'C-05'
  | 'C-06'
  | 'C-07'
  | 'C-08'
  | 'C-09'
  | 'C-10';

export interface CoachSuggestion {
  id: string;
  ruleId: CoachRuleId;
  action: CoachAction;
  exerciseId: string | null;
  title: string;
  explanation: string;
  suggestedWeightKg: number | null;
  blocked: boolean;
  confidence: 'high' | 'medium' | 'low';
}

export interface WorkSetSample {
  reps: number;
  rir: number | null;
  weightKg: number;
}

export interface ExerciseSessionSample {
  date: string;
  maxWeightKg: number;
  maxE1rm: number | null;
  workSets: WorkSetSample[];
  repsTarget: number | null;
  setsTarget: number | null;
  isDeload?: boolean;
}

export interface ExerciseProgressionInput {
  exerciseId: string;
  name: string;
  incrementKg: number;
  loadSemantics: LoadSemantics;
  minReps: number;
  maxReps: number;
  targetRirMin: number;
  targetRirMax: number;
  sessions: ExerciseSessionSample[];
  recentPainHigh: boolean; // douleur ≥ 5 sur dernière séance globale
  phase: PhaseCode | null;
}

export interface ExerciseProgressionResult {
  status: 'increase' | 'hold' | 'decrease' | 'repeat' | 'watch';
  currentLoadKg: number | null;
  suggestedLoadKg: number | null;
  targetReps: [number, number];
  targetRir: [number, number];
  confidence: 'high' | 'medium' | 'low';
  consecutiveSuccesses: number;
  consecutiveFailures: number;
  lastReason: string;
  comparableHistory: boolean;
  ruleId: CoachRuleId;
  blocked: boolean;
}
```

Étendre `CoachExercise` pour fournir `workSets` (ou mapper depuis `workSetReps` + `averageRir` en transition — **préférer `workSets`** dans les nouveaux tests ; adapter `progress.repo` en Task 4).

- [ ] **Step 1: Write / rewrite failing tests for week cases**

Remplacer le cœur de `coach.test.ts` par (garder aussi tests C-04/C-06) :

```ts
import { describe, expect, it } from 'vitest';
import { evaluateExerciseProgression, evaluateCoach } from '../../src/domain/coach';

const base = {
  exerciseId: 'ex',
  name: 'Exo',
  incrementKg: 5,
  loadSemantics: 'external_weight' as const,
  minReps: 10,
  maxReps: 12,
  targetRirMin: 1,
  targetRirMax: 2,
  recentPainHigh: false,
  phase: 'progression' as const,
};

describe('evaluateExerciseProgression', () => {
  it('Smith 65×12 @ RIR 0 → hold, jamais increase', () => {
    const r = evaluateExerciseProgression({
      ...base,
      name: 'Smith squat',
      sessions: [
        {
          date: '2026-09-16',
          maxWeightKg: 65,
          maxE1rm: null,
          workSets: [
            { reps: 12, rir: 0, weightKg: 65 },
            { reps: 10, rir: 0, weightKg: 65 },
          ],
          repsTarget: 12,
          setsTarget: 2,
        },
      ],
    });
    expect(r.status).not.toBe('increase');
    expect(['hold', 'decrease']).toContain(r.status);
  });

  it('Leg curl 60×15 @ RIR 3 → repeat ou increase selon historique', () => {
    const one = evaluateExerciseProgression({
      ...base,
      maxReps: 15,
      minReps: 12,
      sessions: [
        {
          date: '2026-09-14',
          maxWeightKg: 60,
          maxE1rm: null,
          workSets: [
            { reps: 15, rir: 3, weightKg: 60 },
            { reps: 15, rir: 3, weightKg: 60 },
            { reps: 15, rir: 3, weightKg: 60 },
          ],
          repsTarget: 15,
          setsTarget: 3,
        },
      ],
    });
    // RIR 3 > targetRirMax 2 → HOLD (reps hautes mais RIR trop élevé pour « bonne » stricte)
    // Spec analyse : augmenter. Ajuster isGoodSession : RIR >= targetRirMin suffit si reps >= max (pas besoin RIR <= max pour INCREASE path via HOLD→… )
    // Décision d’implémentation figée ici :
    // « bonne » = reps>=maxReps && rir!=null && rir>=targetRirMin && rir!==0 && aucune série rir===0
    // (RIR 3 compte comme bonne intensité sous-maximale → REPEAT sur 1 séance)
    expect(one.status).toBe('repeat');
  });

  it('Leg press 190×13 @ RIR 1 → hold', () => {
    const r = evaluateExerciseProgression({
      ...base,
      maxReps: 15,
      minReps: 12,
      incrementKg: 10,
      sessions: [
        {
          date: '2026-09-14',
          maxWeightKg: 190,
          maxE1rm: null,
          workSets: [{ reps: 13, rir: 1, weightKg: 190 }],
          repsTarget: 12,
          setsTarget: 1,
        },
      ],
    });
    expect(r.status).toBe('hold');
  });

  it('Face pull 50→25 → watch', () => {
    const r = evaluateExerciseProgression({
      ...base,
      sessions: [
        { date: '2026-09-01', maxWeightKg: 50, maxE1rm: null, workSets: [{ reps: 12, rir: 2, weightKg: 50 }], repsTarget: 12, setsTarget: 1 },
        { date: '2026-09-08', maxWeightKg: 50, maxE1rm: null, workSets: [{ reps: 12, rir: 2, weightKg: 50 }], repsTarget: 12, setsTarget: 1 },
        { date: '2026-09-18', maxWeightKg: 25, maxE1rm: null, workSets: [{ reps: 12, rir: 1, weightKg: 25 }], repsTarget: 12, setsTarget: 1 },
      ],
    });
    expect(r.status).toBe('watch');
    expect(r.comparableHistory).toBe(false);
    expect(r.confidence).toBe('low');
  });

  it('douleur élevée → watch', () => {
    const r = evaluateExerciseProgression({
      ...base,
      recentPainHigh: true,
      sessions: [
        {
          date: '2026-09-14',
          maxWeightKg: 100,
          maxE1rm: null,
          workSets: [{ reps: 12, rir: 2, weightKg: 100 }],
          repsTarget: 12,
          setsTarget: 1,
        },
      ],
    });
    expect(r.status).toBe('watch');
  });

  it('C-07 bloque hausse > 10 % / 4 semaines', () => {
    const good = (date: string, w: number) => ({
      date,
      maxWeightKg: w,
      maxE1rm: null,
      workSets: [{ reps: 12, rir: 2, weightKg: w }],
      repsTarget: 12,
      setsTarget: 1,
    });
    const r = evaluateExerciseProgression({
      ...base,
      sessions: [good('2026-08-15', 90), good('2026-09-01', 100), good('2026-09-08', 100)],
    });
    // 2 bonnes → increase proposé 105 mais baseline 90 → >10% → blocked hold/increase blocked
    expect(r.blocked || r.suggestedLoadKg == null || r.status !== 'increase' || r.ruleId === 'C-07').toBe(true);
  });
});
```

**Règle « séance bonne » (figée pour le plan) :**

```text
toutes séries work :
  reps >= maxReps
  AND rir != null
  AND rir >= targetRirMin
  AND rir !== 0
  AND aucune série avec rir === 0
```

(RIR 3 avec reps ≥ max = bonne → REPEAT puis INCREASE ; aligne leg curl de l’analyse.)

- [ ] **Step 2: Run — expect FAIL**

Run: `npx vitest run tests/domain/coach.test.ts`

- [ ] **Step 3: Implement `evaluateExerciseProgression` + adapter `evaluateCoach`**

Pseudo-ordre dans `evaluateExerciseProgression` :

1. Filtrer deload ; si pas de session → hold confiance low  
2. `comparableHistory = isLoadHistoryComparable(sessions.map maxWeight)` ; si false → watch  
3. Si `recentPainHigh` → watch  
4. Compter séries last avec `rir === 0` ; si ≥ 2 → decrease  
5. Si 1 série rir===0 ou reps dans plage / RIR bas → hold  
6. Compter trailing « bonnes » séances → 1 = repeat, ≥2 = increase (sauf readaptation / C-07)  
7. Remplir `lastReason` FR toujours  

`evaluateCoach` : pour chaque exercice, appeler progression → pousser `CoachSuggestion` ; conserver C-04/C-05/C-06 globaux.

Mettre à jour les anciens tests C-01 : sessions doivent avoir `workSets` avec RIR 2 et reps ≥ cible **deux fois** pour encore attendre une hausse.

- [ ] **Step 4: Run — expect PASS**

Run: `npx vitest run tests/domain/coach.test.ts`

- [ ] **Step 5: Commit**

```bash
git add app/src/domain/coach.ts app/tests/domain/coach.test.ts
git commit -m "feat(coach): RIR-centric progression engine"
```

---

### Task 4: Repo memory + branchement snapshot / completeWorkout

**Files:**
- Create: `app/src/repositories/exercise-memory.repo.ts`
- Modify: `app/src/repositories/progress.repo.ts`
- Modify: `app/src/repositories/workouts.repo.ts` (`completeWorkout`)
- Create: `app/tests/repositories/exercise-memory.test.ts` (si setup Dexie déjà utilisé ailleurs — sinon tester via domaine uniquement et smoke repo minimal)

**Interfaces:**
- Consumes: `evaluateExerciseProgression`, données workouts/sets
- Produces:
  - `export async function recomputeAllExerciseMemories(now?: Date): Promise<void>`
  - `export async function getMemoriesByExerciseIds(ids: string[]): Promise<Map<string, ExerciseProgressionMemory>>`

- [ ] **Step 1: Implement `recomputeAllExerciseMemories`**

Algorithme :

1. Charger workouts completed, workoutExercises, setLogs work, exercises  
2. Grouper sets par exercice + workout  
3. Pour chaque exercice avec ≥ 1 séance : construire `ExerciseProgressionInput` (min/max reps depuis `exercise.minReps/maxReps` ou `repsTarget` dernière prescription / défaut 8–12 ; RIR défaut 1–2 ; `loadSemantics` défaut `external_weight`)  
4. `recentPainHigh` = dernière workout completed a `painLevel >= 5`  
5. `evaluateExerciseProgression` → `db.exerciseProgressionMemory.put({ exerciseId, …, updatedAt })`  
6. `getProgressSnapshot` : après build context, appeler `recomputeAllExerciseMemories` **ou** s’assurer que les suggestions viennent du même `evaluateCoach` et upsert memory  

Dans `completeWorkout`, après update status :

```ts
await recomputeAllExerciseMemories();
```

Adapter le mapping `CoachExercise` dans `progress.repo.ts` pour passer `workSets: { reps, rir, weightKg }[]` (filtrer `setKind === 'work'` ou `!isWarmup`).

Étendre `CoachSuggestion` consumers : fournir `confidence: 'medium'` minimum partout pour typecheck.

- [ ] **Step 2: Typecheck + vitest coach + memory smoke**

Run: `npm run typecheck` && `npx vitest run tests/domain/coach.test.ts`

- [ ] **Step 3: Commit**

```bash
git add app/src/repositories/exercise-memory.repo.ts app/src/repositories/progress.repo.ts app/src/repositories/workouts.repo.ts app/tests/repositories/exercise-memory.test.ts
git commit -m "feat(coach): persist and refresh exercise progression memory"
```

---

### Task 5: Seed `loadSemantics`

**Files:**
- Modify: `app/src/data/exercices.seed.json`
- Modify: `app/src/db/seed.ts`

- [ ] **Step 1: Add fields to seed JSON**

Pour **tous** les exercices `loadType === "weight"` : `"loadSemantics": "external_weight"` (ou `machine_resistance` si `equipment === "machine"` — choix plan : machines → `machine_resistance`, free weights → `external_weight`, bodyweight load → `bodyweight`).

Pour `ex-dips-assistes` :

```json
"loadSemantics": "assistance",
"minReps": 8,
"maxReps": 15,
"targetRirMin": 1,
"targetRirMax": 2
```

- [ ] **Step 2: Bump seed + map in `seed.ts`**

```ts
const CURRENT_SEED_VERSION = 2;
```

Mapper `loadSemantics`, `minReps`, `maxReps`, `targetRirMin`, `targetRirMax` depuis le JSON.

Upgrade installations existantes : dans `seedIfNeeded`, si `seedVersion < 2`, **patch** les exercices déjà présents (`db.exercises.update`) pour renseigner `loadSemantics` sans tout reseter — ou migration dédiée `migrate-coach-v2-seed.ts` appelée depuis `main.tsx`. Préférer patch idempotent :

```ts
export async function ensureExerciseLoadSemantics(): Promise<void> {
  const all = await db.exercises.toArray();
  for (const ex of all) {
    if (ex.loadSemantics) continue;
    const semantics =
      ex.id === 'ex-dips-assistes'
        ? 'assistance'
        : ex.equipment === 'machine'
          ? 'machine_resistance'
          : ex.equipment === 'bodyweight'
            ? 'bodyweight'
            : 'external_weight';
    await db.exercises.update(ex.id, { loadSemantics: semantics, updatedAt: nowIso() });
  }
}
```

Appeler depuis `main.tsx` après `seedIfNeeded`.

- [ ] **Step 3: Commit**

```bash
git add app/src/data/exercices.seed.json app/src/db/seed.ts app/src/main.tsx
git commit -m "feat(seed): loadSemantics and assisted dips mapping"
```

---

### Task 6: UI Progression (`CoachSuggestions`)

**Files:**
- Modify: `app/src/features/progress/CoachSuggestions.tsx`
- Modify: `app/src/features/progress/CoachSuggestions.module.css`

- [ ] **Step 1: Afficher action + raison ; accepter HOLD**

- Badge texte : `Consolider` / `Augmenter` / `Réduire` / `Répéter` / `Surveiller` selon `action`  
- Toujours montrer `explanation`  
- Bouton « Utiliser… » si `suggestedWeightKg != null && !blocked && action ∈ increase|decrease|hold`  
- Afficher `confidence` en petit (`haute` / `moyenne` / `faible`)

Labels :

```ts
const ACTION_LABEL: Record<string, string> = {
  increase: 'Augmenter',
  hold: 'Consolider',
  decrease: 'Réduire',
  repeat: 'Répéter',
  watch: 'Surveiller',
  deload: 'Deload',
  vary: 'Varier',
  inform: 'Info',
};
```

- [ ] **Step 2: Smoke manuel / typecheck**

Run: `npm run typecheck`

- [ ] **Step 3: Commit**

```bash
git add app/src/features/progress/CoachSuggestions.tsx app/src/features/progress/CoachSuggestions.module.css
git commit -m "feat(progress): show hold/watch coach actions with reasons"
```

---

### Task 7: Brief pré-séance (Today)

**Files:**
- Create: `app/src/features/today/SessionBrief.tsx`
- Create: `app/src/features/today/SessionBrief.module.css`
- Modify: `app/src/features/today/TodayScreen.tsx`

**Interfaces:**
- Consumes: `getMemoriesByExerciseIds`, prescribed items du jour

- [ ] **Step 1: Component**

```tsx
export function SessionBrief({
  lines,
  painWatch,
}: {
  lines: { name: string; text: string }[];
  painWatch: boolean;
}) {
  if (lines.length === 0 && !painWatch) return null;
  return (
    <section className={styles.brief} aria-label="Objectifs du jour">
      <h2 className={styles.title}>Objectifs du jour</h2>
      {painWatch ? (
        <p className={styles.watch}>Surveiller la douleur déclarée récemment avant d’augmenter les charges.</p>
      ) : null}
      <ul className={styles.list}>
        {lines.slice(0, 6).map((line) => (
          <li key={line.name}><strong>{line.name}</strong> — {line.text}</li>
        ))}
      </ul>
    </section>
  );
}
```

Dans `TodayScreen`, après chargement items :

```ts
const ids = items.map(i => i.exerciseId).filter(Boolean);
const memories = await getMemoriesByExerciseIds(ids);
// si memory vide, await recomputeAllExerciseMemories() puis recharger
```

Construire `text` depuis memory :  
`status hold` → `Maintiens X kg, vise a–b reps à RIR c–d`  
sinon `lastReason` tronqué.

`painWatch` : lire dernier workout completed `painLevel >= 5` (petit helper repo ou query inline).

- [ ] **Step 2: Typecheck**

- [ ] **Step 3: Commit**

```bash
git add app/src/features/today/SessionBrief.tsx app/src/features/today/SessionBrief.module.css app/src/features/today/TodayScreen.tsx
git commit -m "feat(today): show pre-session coach brief"
```

---

### Task 8: Hint mid-set + `coach-midset`

**Files:**
- Create: `app/src/domain/coach-midset.ts`
- Create: `app/tests/domain/coach-midset.test.ts`
- Modify: `app/src/features/session/ActiveSessionScreen.tsx`
- Modify: `app/src/features/session/ActiveSessionScreen.module.css`

**Interfaces:**
- Produces: `export function midSetHint(input: { rir: number | null; reps: number; maxReps: number; setKind: SetKind }): string | null`

- [ ] **Step 1: Tests**

```ts
import { describe, expect, it } from 'vitest';
import { midSetHint } from '../../src/domain/coach-midset';

describe('midSetHint', () => {
  it('RIR 0 → ne pas monter', () => {
    expect(midSetHint({ rir: 0, reps: 12, maxReps: 12, setKind: 'work' })).toMatch(/Ne monte pas/i);
  });
  it('RIR 3 + reps hautes → série facile', () => {
    expect(midSetHint({ rir: 3, reps: 15, maxReps: 12, setKind: 'work' })).toMatch(/facile/i);
  });
  it('warmup → null', () => {
    expect(midSetHint({ rir: 0, reps: 12, maxReps: 12, setKind: 'warmup' })).toBeNull();
  });
});
```

- [ ] **Step 2: Implement + wire**

```ts
export function midSetHint(input: {
  rir: number | null;
  reps: number;
  maxReps: number;
  setKind: SetKind;
}): string | null {
  if (input.setKind !== 'work') return null;
  if (input.rir === 0) {
    return 'Ne monte pas la charge. Prochaine série : vise la plage sans aller à l’échec.';
  }
  if (input.rir != null && input.rir >= 3 && input.reps >= input.maxReps) {
    return 'Série facile. Tu peux confirmer la charge ou augmenter légèrement en fin d’exercice.';
  }
  return null;
}
```

Dans `ActiveSessionScreen`, state `setHint: string | null` ; dans `handleValidate` après `logSet`, si work :

```ts
setSetHint(midSetHint({ rir, reps, maxReps: currentItem?.repsTarget ?? 12, setKind }));
```

Afficher `<p className={styles.setHint} role="status">{setHint}</p>` au-dessus des actions ; clear au changement d’exercice / série suivante (déjà remplacé).

- [ ] **Step 3: Run tests**

Run: `npx vitest run tests/domain/coach-midset.test.ts`

- [ ] **Step 4: Commit**

```bash
git add app/src/domain/coach-midset.ts app/tests/domain/coach-midset.test.ts app/src/features/session/ActiveSessionScreen.tsx app/src/features/session/ActiveSessionScreen.module.css
git commit -m "feat(session): mid-set coach hints from RIR"
```

---

### Task 9: Docs D7 + ADR-003

**Files:**
- Modify: `docs/01-architecture-fonctionnelle.md` (section D7)
- Modify: `docs/adr/003-coach-moteur-de-regles.md`

- [ ] **Step 1: Update D7 table**

Remplacer/étendre C-01 description : hausse seulement si 2 séances bonnes (reps + RIR), jamais si RIR 0. Ajouter C-08 HOLD, C-09 REPEAT, C-10 WATCH. Pointer la spec `2026-09-20-coach-v2-design.md`.

- [ ] **Step 2: ADR note**

Ajouter section « Évolution 2026-09-20 » : actions élargies, RIR central, confidence ; LLM toujours hors décision.

- [ ] **Step 3: Commit**

```bash
git add docs/01-architecture-fonctionnelle.md docs/adr/003-coach-moteur-de-regles.md
git commit -m "docs: update D7 and ADR-003 for coach v2"
```

---

### Task 10: Vérification finale

- [ ] **Step 1: Full checks**

```bash
cd app
npm run typecheck
npx vitest run tests/domain/load-semantics.test.ts tests/domain/coach.test.ts tests/domain/coach-midset.test.ts
```

Expected: all PASS

- [ ] **Step 2: Manual checklist (iPhone / desktop)**

1. Progression : suggestion HOLD avec raison pour un exo à RIR 0  
2. Today : bloc Objectifs du jour  
3. Séance : après série RIR 0, hint visible  
4. Dips assistés : `loadSemantics` assistance en base  

- [ ] **Step 3: Final commit only if docs/fixes remain** — sinon stop

---

## Spec coverage (self-review)

| Spec requirement | Task |
|---|---|
| load-model / comparabilité / assistance | 1, 5 |
| Memory persistée | 2, 4 |
| Décision WATCH→DECREASE→HOLD→REPEAT→INCREASE | 3 |
| Jamais increase si RIR 0 | 3 |
| Douleur ≥ 5 → watch | 3, 7 |
| C-04 / C-07 conservés | 3 |
| UI post + accept hold | 6 |
| Brief pré-séance | 7 |
| Mid-set | 8 |
| Seed dips assistance | 5 |
| Docs D7 / ADR | 9 |
| Tests cas semaine | 3, 10 |

## Placeholder scan

Aucun TBD volontaire ; règle « séance bonne » explicitée dans Task 3 (RIR ≥ min, pas de plafond max pour qualifier « bonne », afin d’aligner leg curl RIR 3).
