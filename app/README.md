# HERY — app

Projet applicatif. Documentation produit et technique dans [`../docs/`](../docs/).

```bash
npm ci
npm run dev -- --host      # --host obligatoire pour tester depuis le téléphone
```

Scripts : `dev`, `build`, `preview`, `lint`, `typecheck`, `test`, `test:e2e`, `format`.
